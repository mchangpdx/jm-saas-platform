// Store — Solink POS Video Overlay page (매장 — Solink POS 비디오 오버레이 페이지)
// Renders the shared SolinkDashboard component (공유 SolinkDashboard 컴포넌트 렌더링)
import SolinkDashboard from '@/modules/security/components/SolinkDashboard';

export default function StoreSolinkPage() {
  return <SolinkDashboard />;
}
